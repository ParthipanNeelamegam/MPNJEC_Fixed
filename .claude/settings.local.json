{
  "permissions": {
    "allow": [
      "PowerShell(Get-ChildItem -Path \".\" -Force | Select-Object Name, @{N=\"Type\";E={if\\($_.PSIsContainer\\){\"Dir\"}else{\"File\"}}} | Format-Table -AutoSize)",
      "Bash(xargs wc -l)",
      "Bash(npm list *)",
      "Bash(git add *)",
      "Bash(git commit -m 'Fix library fine calculation for non-overdue items in student dashboard *)"
    ]
  }
}
